<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Custom Tabs"
        Height="450"
        Width="800">

    <Window.Resources>
        <Style x:Key="TabHeaderStyle" TargetType="RadioButton">
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Background" Value="#E5E5E5"/>
            <Setter Property="BorderBrush" Value="#C8C8C8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="White"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#4A90E2"/>
                            </Trigger>

                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#F2F2F2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TabContentStyle" TargetType="Border">
            <Setter Property="Visibility" Value="Collapsed"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#C8C8C8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16"/>
        </Style>
    </Window.Resources>

    <Grid Background="#F7F7F7">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Tab headers -->
        <UniformGrid Grid.Row="0" Rows="1" Columns="3">
            <RadioButton x:Name="Tab1"
                         Content="Home"
                         GroupName="Tabs"
                         IsChecked="True"
                         Style="{StaticResource TabHeaderStyle}"/>

            <RadioButton x:Name="Tab2"
                         Content="Reports"
                         GroupName="Tabs"
                         Style="{StaticResource TabHeaderStyle}"/>

            <RadioButton x:Name="Tab3"
                         Content="Settings"
                         GroupName="Tabs"
                         Style="{StaticResource TabHeaderStyle}"/>
        </UniformGrid>

        <!-- Content area -->
        <Grid Grid.Row="1" Margin="0">
            <!-- Home -->
            <Border Style="{StaticResource TabContentStyle}">
                <Border.Style>
                    <Style TargetType="Border" BasedOn="{StaticResource TabContentStyle}">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsChecked, ElementName=Tab1}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>

                <StackPanel>
                    <TextBlock Text="Home View"
                               FontSize="24"
                               FontWeight="SemiBold"
                               Margin="0,0,0,12"/>
                    <TextBlock Text="This is the first tab content."/>
                </StackPanel>
            </Border>

            <!-- Reports -->
            <Border Style="{StaticResource TabContentStyle}">
                <Border.Style>
                    <Style TargetType="Border" BasedOn="{StaticResource TabContentStyle}">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsChecked, ElementName=Tab2}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>

                <StackPanel>
                    <TextBlock Text="Reports View"
                               FontSize="24"
                               FontWeight="SemiBold"
                               Margin="0,0,0,12"/>
                    <TextBlock Text="This is the second tab content."/>
                </StackPanel>
            </Border>

            <!-- Settings -->
            <Border Style="{StaticResource TabContentStyle}">
                <Border.Style>
                    <Style TargetType="Border" BasedOn="{StaticResource TabContentStyle}">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsChecked, ElementName=Tab3}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>

                <StackPanel>
                    <TextBlock Text="Settings View"
                               FontSize="24"
                               FontWeight="SemiBold"
                               Margin="0,0,0,12"/>
                    <TextBlock Text="This is the third tab content."/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
